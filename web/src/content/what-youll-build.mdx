You've built one sealed agent, given it a brain, skills from your own registry, and a soul. The capstone makes it a **system**: an **SRE copilot fleet** — several agents, each owning *one* observability tool and *nothing else*, combined to investigate a real incident and propose a fix you approve.

<FleetOrchestration />

## The idea: a team of read-only specialists

When something breaks, you reach for three signals — **logs, metrics, traces**. So the fleet is three specialists plus a writer:

- **logs** 🔎 — reads **Loki**, and can reach `loki.monitoring.svc:3100` and nothing else.
- **metrics** 📈 — reads **Prometheus**, scoped to `…prometheus…:9090` only.
- **traces** 🧵 — reads **Tempo**, scoped to `tempo.monitoring.svc:3200` only.
- **writer** ✍️ — **no egress at all**; it synthesizes the others' findings into a root-cause report.

Each agent's **policy is specific to its tool** — the metrics agent literally cannot read logs, the writer cannot reach the network. That's not a limitation to work around; it *is* the product: a fleet you can hand a production incident without widening anyone's blast radius, and prove it from the audit log.

<Callout type="note" title="Agents diagnose; humans fix">
These agents are sealed from the in-cluster Kubernetes API — they do <strong>read-only investigation</strong> of telemetry, never cluster administration. So the loop is: the fleet finds the root cause, proposes a change, and a <strong>human approves</strong> before anything is applied. (In production that applied change is a git commit ArgoCD syncs — same shape, just automated delivery.)
</Callout>

## The arc of this part

1. **Spin up & give each a soul** — one command brings the fleet up; each agent gets its own `IDENTITY.md` / `SOUL.md` and a policy scoped to just its backend.
2. **Orchestrate an investigation** — a conductor *in this page* plans over the completions API and routes each step to the right specialist.
3. **Test the fleet** — deploy a sample app, break it, watch it go unhealthy, let the fleet find the cause, and fix it with you in the loop.

<Callout type="rh" title="The demo that lands">
A judge breaks an app with one click, asks the fleet "what happened?", and watches three sealed agents — each provably touching only its own backend — converge on the root cause; then approves the one-line fix. Extensible, useful, and <em>governable</em> — on real OpenShift.
</Callout>
