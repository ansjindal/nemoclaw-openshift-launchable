# Service fronting the OpenClaw gateway pod (the Sandbox labels its pod sandbox=openclaw-sandbox).
apiVersion: v1
kind: Service
metadata:
  name: openclaw
  labels:
    app.kubernetes.io/name: openclaw
spec:
  selector:
    sandbox: openclaw-sandbox
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
